# Fortnite Hub - Root Redirect
# Redirects all requests to public/ directory

RewriteEngine On

# If not already in public directory, redirect there
RewriteCond %{REQUEST_URI} !^/Fortnite/public/
RewriteRule ^(.*)$ public/$1 [L,R=301]
